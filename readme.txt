
You need to create a database for the user.

in this case i named the database registeration 

Then create a table called user with the followng column

id (tur on A_I auto increment)
username  -  varchar(100)
email  -  varchar(100)
password  -  varchar(100)


Then use the files you can just copy the actions in the form to your existing forms.

They are commented so you should understand the process

This works on Local host. I guess you understand. You need to put into xammp first before running scripts.

Also you need to install node js and run npm install on the folder from terminal.
